# Database Configuration
DATABASE_URL=postgresql://user:password@localhost:5432/programador-musical

# Security
SECRET_KEY=your-secret-key-here-change-this-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=30

# API Configuration
API_V1_STR=/api/v1
PROJECT_NAME=Programador Musical

# CORS
BACKEND_CORS_ORIGINS=["http://localhost:3000"]

# AWS Cognito Configuration
COGNITO_USER_POOL_ID=your-cognito-user-pool-id
COGNITO_CLIENT_ID=your-cognito-client-id
COGNITO_REGION=us-east-1
COGNITO_DOMAIN=your-cognito-domain-url

# AWS SES Configuration (para envío de emails)
SES_FROM_EMAIL=noreply@programador-musical.com
