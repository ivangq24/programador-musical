import { apiClient } from '../../index';

// Placeholder para Giros API
export const getGiros = async () => {
  return [];
};

export const getGiroById = async (id) => {
  return null;
};

export const createGiro = async (data) => {
  return data;
};

export const updateGiro = async (id, data) => {
  return data;
};

export const deleteGiro = async (id) => {
  return { message: 'Giro eliminado' };
};

