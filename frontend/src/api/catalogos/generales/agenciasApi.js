import { apiClient } from '../../index';

// Placeholder para Agencias API
export const getAgencias = async () => {
  return [];
};

export const getAgenciaById = async (id) => {
  return null;
};

export const createAgencia = async (data) => {
  return data;
};

export const updateAgencia = async (id, data) => {
  return data;
};

export const deleteAgencia = async (id) => {
  return { message: 'Agencia eliminada' };
};

