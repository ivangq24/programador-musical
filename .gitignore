# Environment files (NO subir archivos .env con valores reales)
.env
.env.*
!.env.example
!.env.*.example
!.env.prod.example
.env.local
.env.prod
.env.production
.env.staging
.env.development
.env.test
.env.development.local
.env.test.local
.env.production.local

# Terraform
# Directorio de plugins y módulos
aws/terraform/.terraform/
aws/terraform/.terraform.*/

# Archivos de estado (contienen información sensible)
aws/terraform/*.tfstate
aws/terraform/*.tfstate.*
aws/terraform/*.tfstate.backup
**/*.tfstate
**/*.tfstate.*

# Archivos de variables con valores reales (NO subir)
# IMPORTANTE: Las excepciones deben ir después de las reglas generales
**/*.tfvars
!**/*.tfvars.example
!**/terraform.tfvars.example
aws/terraform/terraform.tfvars
aws/terraform/*.tfvars.json

# Archivos de plan
aws/terraform/*.tfplan
aws/terraform/tfplan
aws/terraform/*.tfplan.*
**/*.tfplan

# Lock file (a veces se incluye, pero mejor ignorarlo por seguridad)
aws/terraform/.terraform.lock.hcl

# Archivos de override
**/*.override.tf
**/*.override.tfvars

# Archivos de crash
aws/terraform/crash.log
aws/terraform/crash.*.log

# Archivos de debug
aws/terraform/debug.tf
aws/terraform/debug.tfvars

# Docker
.dockerignore

# Logs
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Coverage directory used by tools like istanbul
coverage/
*.lcov

# nyc test coverage
.nyc_output

# Dependency directories
node_modules/
jspm_packages/

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Microbundle cache
.rpt2_cache/
.rts2_cache_cjs/
.rts2_cache_es/
.rts2_cache_umd/

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# parcel-bundler cache (https://parceljs.org/)
.cache
.parcel-cache

# Next.js build output
.next
out

# Nuxt.js build / generate output
.nuxt
dist

# Gatsby files
.cache/
public

# Storybook build outputs
.out
.storybook-out

# Temporary folders
tmp/
temp/

# Editor directories and files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# PyInstaller
*.manifest
*.spec

# Unit test / coverage reports
htmlcov/
.tox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
.hypothesis/
.pytest_cache/

# Jupyter Notebook
.ipynb_checkpoints

# pyenv
.python-version

# celery beat schedule file
celerybeat-schedule

# SageMath parsed files
*.sage.py

# Environments
.venv
env/
venv/
ENV/
env.bak/
venv.bak/

# Spyder project settings
.spyderproject
.spyproject

# Rope project settings
.ropeproject

# mkdocs documentation
/site

# mypy
.mypy_cache/
.dmypy.json
dmypy.json

# SSL certificates and keys (NUNCA subir)
ssl/
*.pem
*.key
*.crt
*.csr
*.p12
*.pfx
*.jks
*.keystore
*.truststore

# AWS credentials and configuration
.aws/
aws-credentials
aws-config
*.aws
~/.aws/credentials
~/.aws/config

# SSH keys
*.rsa
*.rsa.pub
*.ed25519
*.ed25519.pub
id_rsa*
id_ed25519*
known_hosts
.ssh/

# Backups and database dumps (may contain sensitive data)
backups/
*.sql
*.dump
*.db
*.sqlite
*.sqlite3
*.backup.sql
*.bak
*.backup
*.old

# Local development overrides
docker-compose.override.local.yml
docker-compose.override.yml

# IDE and editor files with potentially sensitive data
.vscode/settings.json
.idea/workspace.xml
*.code-workspace

# Backup and temporary editor files
*~
*.swp
*.swo

# Temporary files
tmp/
temp/
*.tmp

# OS specific files (already covered above, but keeping for clarity)
# Note: Some entries may be duplicated from earlier sections

# Package manager lock files (optional, but sometimes contain paths)
# Uncomment if you want to ignore them:
# package-lock.json
# yarn.lock
# poetry.lock

# Local configuration overrides
config.local.*
settings.local.*
*.local.*
!*.local.example.*